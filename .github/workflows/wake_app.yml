name: Wake Streamlit App

on:
  schedule:
    - cron: "*/15 * * * *"   # runs every 15 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit App
        run: curl -I https://portfolio-app-gkrrcq4pojhbduq2wwi8no.streamlit.app/
