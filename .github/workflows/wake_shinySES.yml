name: Wake Apps

on:
  schedule:
    - cron: "*/15 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit
        run: curl -I https://portfolio-app-gkrrcq4pojhbduq2wwi8no.streamlit.app/ || true

      - name: Ping Shiny
        run: curl -I https://nefelitrianti.shinyapps.io/ses-earnings/ || true
